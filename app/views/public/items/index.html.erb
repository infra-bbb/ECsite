<h2>商品一覧</h2>
<table>
  <thead>
    <tr>
      <th>商品名</th>
    </tr>
  </thead>
  <% @items.each do |item| %>
    <tbody>
      <tr>
        <td><%= link_to item.name, public_item_path(item) %></td>
      </tr>
    </tbody>
  <% end %>
</table>