<h2>商品一覧</h2>
<table>
  <thead>
    <tr>
      <th>商品名</th>
      <th>値段</th>
    </tr>
  </thead>
  <% @items.each do |item| %>
    <tbody>
      <tr>
        <td><%= item.name %></td>
        <td><%= item.price %> 円</td>
      </tr>
    </tbody>
  <% end %>
</table>